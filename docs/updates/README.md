# Update Manifests

The private QuickPrinter release script creates `alpha.json`, `beta.json` and `pro.json` here when a channel is staged for release.

Each manifest is a static JSON document consumed by the corresponding application channel.
